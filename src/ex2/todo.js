class Todo {
  constructor(value, id) {
    this.id = id;
    this.value = value;
    this.toBeDone = true;
    // this.completeTask;
  }

//   set completeTaskFunc(completeTaskFunc) {
//     this.completeTask = completeTaskFunc;
//     const checkboxBtn = document.querySelector(`#input-${this.id}`);
//     // const taskTxt = document.querySelector(`#task-txt-${this.id - 1}`);
//     checkboxBtn.addEventListener("click", () => this.completeTask(this.id));
//   }
}
