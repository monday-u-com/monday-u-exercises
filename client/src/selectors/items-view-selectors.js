// const getItemsView = state => state.itemsView;
const getLoaderState = (state) => state.itemsView;
export const getLoaderValue = (state) => getLoaderState(state).loaderShow;

// const getCounterState = state => state.counter;

// export const getCounterValue = state => getCounterState(state).value;
